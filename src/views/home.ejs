<%- include('/partials/head') %>

<section class="hero page-pad">
  <p class="eyebrow">A small shop, honestly run</p>
  <h1>Things made well, sold plainly.</h1>
  <p class="lede">Cartward is a demonstration storefront. The shelves are stocked and the
  checkout works — payments are sandbox-only, so nothing is ever charged.</p>
</section>

<%- include('/partials/footer') %>
